import { makeStyles } from "@mui/styles";

export default makeStyles(() => {
  return {
    root: {
      flexGrow: 1,
      padding: "10px",
      backgroundColor: "silver",
      margin: "4px",
    },
    paper: {
      padding: "10px",
      textAlign: "center",
    },
  };
});
